import api from "../api";

const studentFeeApi = {
    // Fee-related student APIs are now handled through the student ledger system
    // via FeeCollectionService → FeeCalculationEngine
};

export default studentFeeApi;
