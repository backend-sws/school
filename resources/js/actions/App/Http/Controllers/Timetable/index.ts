import TimetablePageController from './TimetablePageController'
const Timetable = {
    TimetablePageController: Object.assign(TimetablePageController, TimetablePageController),
}

export default Timetable